/**
 * Vercel Entry Point (Bundled Proxy)
 * This file will be replaced by api/index.js during 'pnpm run vercel-build'
 * But we keep it as a fallback or for local Vercel CLI testing.
 */

import app from "./index.js";
export default app;
